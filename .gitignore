# Gradle
.gradle/
build/
app/build/

# Local machine config (SDK path)
local.properties

# Signing secrets — keep the keystore AND its passwords out of the repo
*.keystore
*.jks
keystore.properties

# IDE
.idea/
*.iml
.kotlin/
captures/

# OS
Thumbs.db
.DS_Store

# Local tooling / build artifacts not for the repo
.claude/
HANDOFF.md
/prezentacija/
/Racunko-prezentacija.zip
*.apk

# OCR models are fetched at build time (tessdata_fast), never committed.
# v1.7: both flavors bundle them, so they live in the shared source set.
/app/src/main/assets/tessdata/
